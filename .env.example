# Example environment variables for FreemanNotes
# Copy this file to .env and fill in real values (DO NOT commit secrets to git)

# Server
PORT=
APP_URL=
NODE_ENV=

# Database (MySQL)
# Provide a single `DATABASE_URL` in your local `.env` (do NOT commit secrets).
# Format: mysql://user:password@host:port/database
DATABASE_URL=

# Security
# Use a long, random string for `JWT_SECRET` in production and do NOT commit it.
JWT_SECRET=
USER_REGISTRATION_ENABLED=

# SMTP (optional)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
INVITE_FROM=
APP_BASE_URL=
# Optional: public production URL for invite links. Can be a hostname or full URL.
# PRODUCTION_URL=

VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=https://freeman.yourdomain.com

# Uploads
# Maximum JSON body size for image uploads (in MB). Increase for large data URLs/files.
MAX_IMAGE_UPLOAD_MB=

# Where the server stores uploaded files (avatars + note images persisted from data URLs).
# The server serves these files at URLs under `/uploads/...`.
#
# In Docker/Unraid you usually want this on a bind-mounted host path.
# - UPLOADS_DIR is the container path (must match your volume target).
# - UPLOADS_VOLUME is the Compose volume source (named volume or absolute host path).
UPLOADS_DIR=
UPLOADS_VOLUME=

# OCR
# Controls server-side OCR logging verbosity.
# Values: silent | error | warn | info | debug | trace
OCR_LOG_LEVEL=

# Notes:
# - `.env.example` must never contain real secret values. Keep this file in source control.
# - Put real secrets in `.env` (which is ignored) or in your deployment environment.
